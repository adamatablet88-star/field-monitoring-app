# מפת דרכים

## מה כבר קיים (פרוטוטייפים, לפי מסמך הצוות)

הפרוטוטייפים הבאים הוצגו/נבנו להדגמה מחוץ למאגר הקוד הזה, ומשמשים בסיס
עיצובי/פונקציונלי לשלב המימוש:

- **דשבורד מגמות** — צפייה בגרפים השוואתיים.
- **קובץ ייצוא לדוח רגולטורי** — Excel לדוגמה תואם דוח אמיתי.
- **מסך הקמת אתר (Admin)** — פרוטוטייפ צפייה בלבד.

כל הפרוטוטייפים הקיימים עבדו על נתוני דמו בזיכרון הדפדפן — ללא שרת,
ללא מסד נתונים אמיתי, ללא שמירה לצמיתות.

## מה עדיין לא נבנה

- טפסי הזנת נתונים בפועל בשטח (עדשת דלק, SVE, Bio-venting, דיגום מי
  תהום).
- שרת ומסד נתונים אמיתיים.
- מנגנון Offline-first אמיתי (מסד מקומי במכשיר + סנכרון) — כרגע מוגדר
  כדרישה בלבד (`docs/architecture.md`), לא ממומש.
- מסך התחברות (Login) אמיתי מול שרת.
- מנגנון נעילה/פתרון קונפליקטים בפועל.

## סדר פיתוח מומלץ

1. **שלד הפרויקט** — הקמת client (React/TS) ו-server (Node/TS) בהתאם
   ל-`docs/architecture.md`, כולל סכמת Postgres הראשונית שמשקפת את
   `docs/data-model.md`.
2. **מסד מקומי + סנכרון בסיסי** — Dexie.js בצד הלקוח, outbox pattern,
   endpoint סנכרון פשוט (ללא פתרון קונפליקטים מתקדם עדיין).
3. **Admin: הקמת אתר/קידוחים/מערכות** — מימוש מלא של מסך הקמת האתר
   (שדרוג מהפרוטוטייפ הקיים), כולל תעודת הזהות המלאה לקידוח ומנוע
   קונפיגורציית הפרמטרים הגמיש (`ParameterConfig`, כולל שכבת הסף
   הקריטי).
4. **טפסי שטח — עדשת דלק** — המודול הפשוט ביותר יחסית; טוב לאמת עליו את
   דפוסי ה-UX המשותפים (Soft Warning, "לא נמדד", "הכל ללא שינוי").
5. **טפסי שטח — SVE ו-Bio-venting** — כולל טיפול קטן/גדול, קידוחי טיפול,
   נקודות ניטור בעומקים משתנים, והתרעת הסף הקריטי.
6. **טפסי שטח — דיגום מי תהום** — כולל לוג הייצוב וחישוב בחירת כלי
   הדיגום.
7. **התחברות אמיתית + RBAC אכיפה בצד שרת**.
8. **מנגנון קונפליקטים מלא** בסנכרון (זיהוי + התרעה למשתמש שני).
9. **ניהול תדירויות ומעקב רגולטורי** (Audit Trail, "מה נדרש החודש",
   סטטוסי דוחות).
10. **דשבורד מגמות וייצוא Excel** — שדרוג הפרוטוטייפים הקיימים למקור
    נתונים אמיתי (במקום דמו).

הסדר מבוסס על תלות טכנית (שלד → מסד → הקמת אתר לפני טפסים שתלויים
בקונפיגורציה שהוגדרה ב-Admin) ועל עדיפות עסקית (טפסי השטח הם הליבה
היומיומית של הטכנאים).
